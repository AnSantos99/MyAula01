using System;

namespace TiposVarios
{
    class Program
    {
        static void Main(string[] args)
        {
            //Integers of diferent types.
            int num_inteiro = 4;
            uint unsign_int = 4U;
            long long_int = 4L;
            ulong long_unsignint = 4UL;

            Console.WriteLine(num_inteiro);
            Console.WriteLine(unsign_int);
        }
    }
}
